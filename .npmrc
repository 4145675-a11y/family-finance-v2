# Reproducible, project-local installs only (ADR-0001).
# cache is pinned inside the project so npm never reads or writes outside the repo.
cache=.npm-cache
save-exact=true
engine-strict=true
fund=false
